# Copy this to .env and fill in your values
# NEVER commit .env to version control

FLASK_ENV=development
SECRET_KEY=muda-isto-para-algo-secreto-e-longo
PORT=5001

# Default is SQLite (instance/anossa.db) — no setup needed.
# Uncomment to use PostgreSQL instead (percent-encode special characters in the password):
# DATABASE_URL=postgresql://user:password@localhost:5432/anossaapp
